// TW Snapshot
// Input SHA-256: e652f448fbf09a37afacb03afa9f3448cce43aef06439b58b44aeec73edb0438

// Sprite1 script
(function factoryXYZ(thread) { const target = thread.target; const runtime = target.runtime; const stage = runtime.getTargetForStage();
const b0 = stage.variables["Go=PJS7BFXYo_qi2S:kQ"];
return function* genXYZ () {
b0.value = 0;
while (true) {
b0.value = (toNotNaN(+b0.value) + 1);
thread.timer = timer();
var a0 = Math.max(0, 1000 * -1);
runtime.requestRedraw();
yield;
while (thread.timer.timeElapsed() < a0) {
yield;
}
thread.timer = null;
yield;
}
retire(); return;
}; })

// Sprite1 script
(function factoryXYZ(thread) { const target = thread.target; const runtime = target.runtime; const stage = runtime.getTargetForStage();
return function* genXYZ () {
runtime.ext_scratch3_looks._say("plan 3", target);
yield* thread.procedures["Znon warp"]();
runtime.stopForTarget(target, thread);
runtime.ext_scratch3_looks._say("end", target);
retire(); return;
}; })

// Sprite1 Znon warp
(function factoryXYZ(thread) { const target = thread.target; const runtime = target.runtime; const stage = runtime.getTargetForStage();
const b0 = stage.variables["Go=PJS7BFXYo_qi2S:kQ"];
return function* genXYZ_non_warp () {
b0.value = 0;
for (var a0 = 5; a0 > 0; a0--) {
thread.timer = timer();
var a1 = Math.max(0, 1000 * 0);
runtime.requestRedraw();
yield;
while (thread.timer.timeElapsed() < a1) {
yield;
}
thread.timer = null;
yield;
}
if ((toNotNaN(+b0.value) === 5)) {
runtime.ext_scratch3_looks._say("pass non warp 1", target);
}
if ((("" + (yield* thread.procedures["Wverify runs warp %s"]((yield* thread.procedures["Zverify runs non warp %s"]((yield* thread.procedures["Wverify runs warp %s"]("abc"))))))).toLowerCase() === "warp: non warp: warp: abc".toLowerCase())) {
runtime.ext_scratch3_looks._say("pass non warp and warp mix", target);
}
b0.value = 0;
for (var a2 = 5; a2 > 0; a2--) {
thread.timer = timer();
var a3 = Math.max(0, 1000 * 0);
runtime.requestRedraw();
yield;
while (thread.timer.timeElapsed() < a3) {
yield;
}
thread.timer = null;
yield;
}
if ((toNotNaN(+b0.value) === 5)) {
runtime.ext_scratch3_looks._say("pass non warp 2", target);
}
return "";
}; })

// Sprite1 Wverify runs warp %s
(function factoryXYZ(thread) { const target = thread.target; const runtime = target.runtime; const stage = runtime.getTargetForStage();
const b0 = stage.variables["Go=PJS7BFXYo_qi2S:kQ"];
return function* genXYZ_verify_runs_warp_ (p0) {
b0.value = 0;
for (var a0 = 5; a0 > 0; a0--) {
thread.timer = timer();
var a1 = Math.max(0, 1000 * 0);
runtime.requestRedraw();
while (thread.timer.timeElapsed() < a1) {
if (isStuck()) yield;
}
thread.timer = null;
if (isStuck()) yield;
}
if (!compareEqual(b0.value, 0)) {
runtime.ext_scratch3_looks._say("fail did not run warp", target);
}
return ("warp: " + ("" + p0));
return "";
}; })

// Sprite1 Zverify runs non warp %s
(function factoryXYZ(thread) { const target = thread.target; const runtime = target.runtime; const stage = runtime.getTargetForStage();
const b0 = stage.variables["Go=PJS7BFXYo_qi2S:kQ"];
return function* genXYZ_verify_runs_non_warp (p0) {
b0.value = 0;
for (var a0 = 5; a0 > 0; a0--) {
thread.timer = timer();
var a1 = Math.max(0, 1000 * 0);
runtime.requestRedraw();
yield;
while (thread.timer.timeElapsed() < a1) {
yield;
}
thread.timer = null;
yield;
}
if (compareEqual(b0.value, 0)) {
runtime.ext_scratch3_looks._say("fail ran warp", target);
}
return ("non warp: " + ("" + p0));
return "";
}; })
